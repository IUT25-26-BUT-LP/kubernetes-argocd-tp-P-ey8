apiVersion: v1
kind: Service
metadata:
  name: app-securisee-service
  namespace: default
  labels:
    app: app-securisee
spec:
  type: LoadBalancer 
  selector:
    app: app-securisee
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080